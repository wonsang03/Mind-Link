spring.application.name=demo
server.port=8081

# =====================================================
# 로그 파일 — 관리자 페이지 실시간 뷰어용 (출력 패턴은 logback-spring.xml)
# logback-spring.xml 의 LogMaskingConverter 로 민감정보 자동 마스킹
# =====================================================
logging.file.name=logs/mindlink.log
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=7
logging.logback.rollingpolicy.total-size-cap=200MB

# 프로젝트 루트 .env (Git 미포함, .env.example 참고)
spring.config.import=optional:file:.env[.properties]

# =====================================================
# Oracle DB — 값은 .env 에서 로드
# =====================================================
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@localhost:1521/FREEPDB1}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.username=${DB_USERNAME:APP_USER}
spring.datasource.password=${DB_PASSWORD:AppUser1234}

# JPA (Oracle)
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ====== Thymeleaf (개발 시 캐시 비활성) ======
spring.thymeleaf.cache=false

# ====== 정적 리소스 캐시 비활성 (개발) ======
spring.web.resources.cache.period=0

# Oracle: 샘플 데이터는 sql/ORACLE_SETUP.sql 로 적재 (Spring data.sql 미사용)
spring.sql.init.mode=never

# 파일 업로드 (커뮤니티 첨부)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=110MB
app.upload.dir=uploads

# Spring AI (자동 설정 비활성)
spring.ai.model.openai.enabled=false
spring.ai.vectorstore.oracle.enabled=false
spring.autoconfigure.exclude=\
org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration,\
org.springframework.ai.model.openai.autoconfigure.OpenAiEmbeddingAutoConfiguration,\
org.springframework.ai.model.openai.autoconfigure.OpenAiImageAutoConfiguration,\
org.springframework.ai.model.openai.autoconfigure.OpenAiAudioSpeechAutoConfiguration,\
org.springframework.ai.model.openai.autoconfigure.OpenAiAudioTranscriptionAutoConfiguration,\
org.springframework.ai.model.openai.autoconfigure.OpenAiModerationAutoConfiguration,\
org.springframework.ai.vectorstore.oracle.autoconfigure.OracleVectorStoreAutoConfiguration

# =====================================================
# 네이버 도서 검색 API — 값은 .env (서상원 맞춤 추천)
# =====================================================
naver.api.client-id=${NAVER_API_CLIENT_ID:NOT_SET}
naver.api.client-secret=${NAVER_API_CLIENT_SECRET:NOT_SET}

# =====================================================
# 네이버 지역검색 OpenAPI (/v1/search/local.json) — 상담소 찾기
# .env 의 NAVER_API_* 와 동일 키 사용 가능 (네이버 앱에서「검색」API 사용 체크)
# =====================================================
# OPENAPI_* 가 비어 있으면 Spring 이 빈 문자열로 고정해 API 키가 안 잡히는 경우가 있어 도서 검색 키와 동일 변수 사용
naver.openapi.client-id=${NAVER_OPENAPI_CLIENT_ID:${NAVER_API_CLIENT_ID:NOT_SET}}
naver.openapi.client-secret=${NAVER_OPENAPI_CLIENT_SECRET:${NAVER_API_CLIENT_SECRET:NOT_SET}}
naver.openapi.local-search-url=https://openapi.naver.com/v1/search/local.json
naver.openapi.display=20

# =====================================================
# Google Gemini API — 도서 추천 전용 (com.mindlink.recommendation)
# =====================================================
gemini.api.key=${GEMINI_API_KEY:NOT_SET}

# =====================================================
# OpenAI API — AI 종합 보고서(위로 편지) 전용 (gpt-4o-mini)
# =====================================================
openai.api.key=${OPENAI_API_KEY:OPENAI_KEY_NOT_SET}
openai.api.url=https://api.openai.com/v1/chat/completions
openai.model=gpt-4o-mini
openai.timeout.seconds=120

# AI 종합 보고서 한도 (사용자당 24시간 N 회)
care-report.daily-limit=3

# =====================================================
# 커뮤니티 3D 클러스터링 (서상원)
# =====================================================
# K-Means K(집단 수). 페르소나 시드의 임시 cluster_id (0~5) 와 맞춰 6 권장.
chat.cluster.k=${CHAT_CLUSTER_K:6}
# 가중 유클리드 거리 — depression 에 약간 더 가중치
chat.cluster.weight.stress=${CHAT_CLUSTER_W_STRESS:1.0}
chat.cluster.weight.depression=${CHAT_CLUSTER_W_DEPRESSION:1.2}
chat.cluster.weight.anxiety=${CHAT_CLUSTER_W_ANXIETY:1.0}
# K-Means 수렴 조건·재시작 횟수
chat.cluster.kmeans.max-iterations=500
chat.cluster.kmeans.tolerance=0.0001
chat.cluster.kmeans.restarts=10
# 시드 메타데이터 (UI 표기에만 사용)
chat.cluster.seed.persona-types=30
chat.cluster.seed.variants-per-persona=7
chat.cluster.seed.total-target=210
# 앱 기동 시 자동 재계산 여부 (DB 가 비어 있을 때는 영향 없음)
chat.cluster.seed-on-startup=${CHAT_CLUSTER_SEED_ON_STARTUP:false}
# 시각화 API 캐시(초) · 최대 포인트 수
chat.cluster.viz.cache-seconds=30
chat.cluster.viz.max-points=500
# recompute 배치 사이즈 (대량 인원 대비)
chat.cluster.recompute.batch-size=200
