# 이 파일을 복사해 .env 로 저장한 뒤 실제 값을 입력하세요.
# .env 는 Git에 올라가지 않습니다.

# Oracle DB
DB_URL=jdbc:oracle:thin:@localhost:1521/FREEPDB1
DB_USERNAME=APP_USER
DB_PASSWORD=your_db_password_here

# 네이버 Open API (https://developers.naver.com) — 도서 검색 + 지역검색(상담소) 동일 앱 키 사용 가능
NAVER_API_CLIENT_ID=your_naver_client_id
NAVER_API_CLIENT_SECRET=your_naver_client_secret
# 지역검색만 별도 키를 쓸 때 (비우면 위 도서 검색 키를 재사용)
NAVER_OPENAPI_CLIENT_ID=
NAVER_OPENAPI_CLIENT_SECRET=

# Google Gemini (https://aistudio.google.com/apikey) — 도서 추천만
GEMINI_API_KEY=your_gemini_api_key

# OpenAI (https://platform.openai.com/api-keys) — AI 종합 보고서(편지)만
OPENAI_API_KEY=sk-your_openai_api_key
CARE_LLM_PROVIDER=openai
